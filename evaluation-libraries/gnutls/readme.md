# gnuttls example with strict sni and strict alpn

Tested with GnuTLS 3.7.1

needs tls-baseimage already in docker

based on https://gitlab.com/gnutls/gnutls/-/tree/3.7.1/doc/examples

```bash
./run.sh
```